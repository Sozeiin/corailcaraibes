# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-key

# Supabase Edge Functions Secrets (configure in Supabase Dashboard)
# Not needed in .env, but documented here for reference

# VAPID Keys for Web Push Notifications
# Generate with: node scripts/generate-vapid.js
# VAPID_PUBLIC_KEY=your-public-vapid-key
# VAPID_PRIVATE_KEY=your-private-vapid-key
# VAPID_MAILTO=mailto:contact@yourdomain.com

# Push Notifications Admin Token
# Generate a strong random token for push admin access
# PUSH_ADMIN_TOKEN=your-strong-admin-token
